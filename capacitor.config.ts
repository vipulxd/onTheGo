import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onTheGo.app',
  appName: 'on-the-go',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
